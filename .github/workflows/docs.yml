name: documentation

on:
  push:
    branches:
      - main
      - phase5-production-deployment
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Build Sphinx documentation
        run: |
          cd docs
          make html
      - name: Create mock results if missing
        run: |
          python scripts/create_mock_results.py --results-dir results || true
      - name: Generate research charts
        run: |
          python scripts/generate_charts.py
      - name: Prepare GitHub Pages content
        run: |
          # Create _site directory with both landing page and Sphinx docs
          mkdir -p _site
          cp index.html _site/
          # Include standalone methodology page at the site root
          if [ -f methodology.html ]; then cp methodology.html _site/; fi
          cp -r assets _site/
          cp -r docs/build/html _site/docs

          # CRITICAL: Disable Jekyll processing (prevents _static deletion)
          touch _site/.nojekyll
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
